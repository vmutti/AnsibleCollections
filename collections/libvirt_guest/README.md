# vmutti.libvirt_guest

Installs the QEMU guest agent inside a libvirt VM. Commonly used as a provisioner during a `vmutti.packer.build` image build for libvirt-based images.

## Roles

- [install](roles/install/README.md) — installs the `qemu-guest-agent` package and enables its service

## License

MIT
