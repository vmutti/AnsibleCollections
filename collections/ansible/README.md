# vmutti.ansible

Installs the Ansible Python package via pip so a host can run Ansible locally (for example, to act as an Ansible controller itself).

## Roles

- [install](roles/install/README.md) — installs the ansible pip package when host_install_ansible is true

## License

MIT
